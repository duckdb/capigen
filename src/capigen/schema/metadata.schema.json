{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Schema for metadata.yaml — defines ABI naming conventions, primitive types, and API versions.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "versions", "suffixes", "primitives"],
  "properties": {
    "schema_version": {
      "description": "IDL schema version that this metadata and its associated modules comply with (semver).",
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "versions": {
      "description": "Supported API versions in semver format.",
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^\\d+\\.\\d+\\.\\d+$"
      },
      "uniqueItems": true
    },
    "suffixes": {
      "description": "ABI naming suffixes applied to each construct type. Every adapter uses these to compute canonical names.",
      "type": "object",
      "additionalProperties": false,
      "required": ["handles", "callbacks", "aliases"],
      "properties": {
        "handles": { "type": "string", "description": "Suffix for opaque handle types (e.g. '_ptr')." },
        "callbacks": { "type": "string", "description": "Suffix for callback function pointers (e.g. '_cb')." },
        "aliases": { "type": "string", "description": "Suffix for type aliases (e.g. '_t')." }
      }
    },
    "primitives": {
      "description": "Primitive type vocabulary with C ABI type names.",
      "type": "array",
      "items": { "$ref": "#/$defs/Primitive" },
      "uniqueItems": true
    }
  },
  "$defs": {
    "Primitive": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "c_type"],
      "properties": {
        "name": {
          "description": "Abstract type name used in the API spec.",
          "type": "string"
        },
        "c_type": {
          "description": "Canonical C type name in the ABI.",
          "type": "string"
        }
      }
    }
  }
}
