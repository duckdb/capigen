{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:capigen:schema:metadata",
  "description": "Schema for metadata.yaml \u2014 defines ABI naming conventions, primitive types, and API versions.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "versions",
    "suffixes",
    "primitives"
  ],
  "properties": {
    "schema_version": {
      "description": "IDL schema version that this metadata and its associated modules comply with. MAJOR.MINOR, or legacy MAJOR.MINOR.PATCH (patch ignored).",
      "type": "string",
      "pattern": "^\\d+\\.\\d+(\\.\\d+)?$"
    },
    "versions": {
      "description": "The described API's known versions (vX.Y.Z), newest last or in any order. Validates 'added' / 'deprecated' on functions. The spec itself describes the API as of the latest of these.",
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^v\\d+\\.\\d+\\.\\d+$"
      },
      "uniqueItems": true,
      "minItems": 1
    },
    "suffixes": {
      "description": "ABI naming suffixes applied to each construct type. Every adapter uses these to compute canonical names.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "handles",
        "callbacks",
        "aliases"
      ],
      "properties": {
        "handles": {
          "type": "string",
          "description": "Suffix for opaque handle types (e.g. '_handle')."
        },
        "callbacks": {
          "type": "string",
          "description": "Suffix for callback function pointers (e.g. '_cb')."
        },
        "aliases": {
          "type": "string",
          "description": "Suffix for type aliases (e.g. '_t')."
        }
      }
    },
    "primitives": {
      "description": "Primitive type vocabulary with C ABI type names.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/Primitive"
      },
      "uniqueItems": true
    },
    "prefix": {
      "description": "String prepended to all generated C identifiers (types, functions, constants). E.g. 'duckdb_v2_' yields 'duckdb_v2_open'; uppercase form used for constants/enum values.",
      "type": "string"
    },
    "lifecycle_states": {
      "description": "The lifecycle states constructs can be in, and how each renders. Only declared states exist; a construct's 'lifecycle' entries must name them.",
      "type": "object",
      "propertyNames": {
        "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
      },
      "additionalProperties": {
        "$ref": "#/$defs/StateDefinition"
      }
    }
  },
  "$defs": {
    "StateDefinition": {
      "description": "When constructs in this state are emitted. always: unconditionally. opt_in: behind #ifdef guard. opt_out: behind #ifndef guard. never: not emitted at all. The guard is the macro token for the gated modes.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "visibility"
      ],
      "properties": {
        "guard": {
          "description": "Macro token for opt_in / opt_out gating.",
          "type": "string",
          "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
        },
        "visibility": {
          "type": "string",
          "enum": [
            "always",
            "opt_in",
            "opt_out",
            "never"
          ]
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "visibility": {
                "enum": [
                  "opt_in",
                  "opt_out"
                ]
              }
            }
          },
          "then": {
            "required": [
              "guard"
            ]
          }
        },
        {
          "if": {
            "properties": {
              "visibility": {
                "enum": [
                  "always",
                  "never"
                ]
              }
            }
          },
          "then": {
            "not": {
              "required": [
                "guard"
              ]
            }
          }
        }
      ]
    },
    "Primitive": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "c_type"
      ],
      "properties": {
        "name": {
          "description": "Abstract type name used in the API spec.",
          "type": "string"
        },
        "c_type": {
          "description": "Canonical C type name in the ABI.",
          "type": "string"
        },
        "underlying": {
          "description": "If present, the generator emits 'typedef <underlying> <c_type>;' in the header preamble before all modules.",
          "type": "string"
        }
      }
    }
  }
}
